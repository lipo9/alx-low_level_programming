#include<stdio.h>
/**
 * main - This is the function that is called at the program startup
 * @void: takes no parameters
 * Description: The main function is the first function tha is executed
 * in the program when the execution is started
 * Return: retuns 0 (int value)
  */
int main(void)
{
	/**
	 * Description: using the built-in function "puts"
	 * to print exactly "Programming is like building a multilingual puzzle
	 *
	 * puts - writes a string to stdout wothout the null character, followed
	 * by a new line
	 */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
